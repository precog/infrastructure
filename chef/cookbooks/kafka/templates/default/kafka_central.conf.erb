# kafka
#
# Queueing system

description	"Kafka Central"

start on filesystem
stop on runlevel S

respawn

script
sudo -u precog /opt/precog/kafka/bin/kafka-server-start.sh /etc/precog/kafka-central-log4j.properties /etc/precog/precog.central-kafka.properties
end script
