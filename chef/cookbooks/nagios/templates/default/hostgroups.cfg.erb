# Hostgroup definitions
# Automatically generated by Chef

define hostgroup {
  hostgroup_name all
  alias all
  members ^dev.*$,^appserver.*$,^mongo.*$,^qclus.*$
}

define hostgroup {
  hostgroup_name default
}

define hostgroup {
  hostgroup_name reportgrid_api
}

<% @roles.each do |role| %>
define hostgroup {
  hostgroup_name <%= role %>
}
<% end %>