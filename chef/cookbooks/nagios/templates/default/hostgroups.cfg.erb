# Hostgroup definitions
# Automatically generated by Chef

define hostgroup {
  hostgroup_name all
  alias all
  members *
}

<% @roles.each do |r| 
  if r =~ /monitoring|appserver/ then -%>
define hostgroup {
  hostgroup_name <%= r %>
  alias <%= r %>
}

<% end 
end -%>

define hostgroup {
  hostgroup_name mongodb-replset-server
  alias mongodb-replset-server
}
