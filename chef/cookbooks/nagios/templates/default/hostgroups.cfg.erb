# Hostgroup definitions
# Automatically generated by Chef

define hostgroup {
  hostgroup_name all
  alias all
  members ^dev.*$,^appserver.*$,^mongo.*$,^qclus.*$,^web.*$,^nebula.*$
}

define hostgroup {
  hostgroup_name default
}

define hostgroup {
  hostgroup_name reportgrid_api
}

define hostgroup {
  hostgroup_name precog_beta_api
}

define hostgroup {
  hostgroup_name precog_nebula_api
}

<% @roles.each do |role| %>
define hostgroup {
  hostgroup_name <%= role %>
}
<% end %>