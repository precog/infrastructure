# Hostgroup definitions
# Automatically generated by Chef

define hostgroup {
  hostgroup_name all
  alias all
  members ^.*$
}

define hostgroup {
  hostgroup_name default
}

<% @roles.each do |role| %>
define hostgroup {
  hostgroup_name <%= role %>
}
<% end %>